// Write an Apex code to print all the odd numbers from 1 to 100 using for loop.    1,3,5,7.....99


Integer oddnum=1;
for(integer count=0;count<=99;count++){
    count+=oddnum;
    System.debug(count);
}