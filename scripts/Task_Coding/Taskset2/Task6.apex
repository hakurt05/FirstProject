Integer leadScore = 50 ; 
Boolean hasEmail, hasPhone , hasTwitter = True ; 

if(hasEmail = True ) { 
    System.debug( 'The lead store is '  + (leadScore+20) ) ; }
if(hasPhone = True ) { 
    System.debug( 'The lead store is '  + (leadScore+18) ) ; }
if(hasTwitter = True ) { 
    System.debug( 'The lead store is '  + (leadScore+12) ) ; }